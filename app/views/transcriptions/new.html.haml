.row
    %h1 Add new transcription

    = form_for :article do |f|
        = f.label :title
        %br
        = f.text_field :title
        = f.label :text
        %br
        = f.text_area :text
        = f.submit :class => 'button'
